export const StatusPanel = () => {
  return (
    <section>
      <h2>Status</h2>
      <p>Placeholder before API integration. Connect this panel using types from `packages/api-types`.</p>
    </section>
  )
}
